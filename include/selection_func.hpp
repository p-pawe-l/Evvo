#pragma once

#include "genome.hpp"

template <typename T>
std::size_t roulette_selection(const PopulationVec<T>& population)
{

}

template <typename T>
std::size_t linear_rank_selection(const PopulationVec<T>& population)
{

}

template <typename T>
std::size_t exp_rank_selection(const PopulationVec<T>& population)
{

}

template <typename T>
std::size_t steady_state_selection(const PopulationVec<T>& population)
{

}

template <typename T>
std::size_t tournament_selection(const PopulationVec<T>& population)
{

}

template <typename T>
std::size_t truncation_selection(const PopulationVec<T>& population)
{

}

template <typename T>
std::size_t elitist_selection(const PopulationVec<T>& population)
{

}

template <typename T>
std::size_t boltzmann_selection(const PopulationVec<T>& population) 
{

}

template <typename T>
std::size_t lexicase_selection(const PopulationVec<T>& population)
{
    
}
